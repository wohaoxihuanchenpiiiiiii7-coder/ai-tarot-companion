# Copy the project_id from the edgespark.toml generated by `edgespark init`.
# Commit the real edgespark.toml after project initialization.
project_id = "your_edgespark_project_id"

[server]
path = "server"

[web]
path = "."
output_path = "dist"
